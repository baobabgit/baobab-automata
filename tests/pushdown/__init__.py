"""
Tests pour le module pushdown - Automates à pile.

Ce module contient tous les tests unitaires pour les automates à pile
non-déterministes (PDA), déterministes (DPDA) et non-déterministes (NPDA).
"""
